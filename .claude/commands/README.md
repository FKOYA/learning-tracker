# .claude/commands/

Claude Code のカスタムスラッシュコマンドを置くディレクトリ。

## 追加するとき

同じ手順を毎回手で指示している場合に、`/コマンド名` で呼び出せるショートカットとして切り出す。まだ該当するものが無いため、現時点ではこのディレクトリにコマンドはない。

## 形式

`.claude/commands/<command-name>.md` として1ファイル1コマンド。Markdown本文がそのままプロンプトとして展開される。
